{
  "name": "notein-viewer",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "build:wasm": "cd ../wasm && zig build && (command -v wasm-opt >/dev/null && wasm-opt --enable-bulk-memory --enable-mutable-globals --enable-nontrapping-float-to-int --enable-sign-ext --enable-simd --enable-multivalue --enable-reference-types -Oz ../web/src/wasm/notein.wasm -o ../web/src/wasm/notein.wasm || true)",
    "dev": "npm run build:wasm && vite",
    "build": "npm run build:wasm && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  },
  "dependencies": {
    "@preact/signals": "^2.11.1",
    "pdf-lib": "^1.17.1",
    "preact": "^10.29.8"
  }
}
